var express = require('express');
var router = express.Router();
var athomeModel = require('./../../models/athomeModel');


router.get('/', async function(req, res, next) {
  state[ ejercicios, setEjercicios] = useState([req.] )
}